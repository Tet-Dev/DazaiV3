import { Member } from 'eris';
export const PermissionNodes = {
  migrateData: 'migrate',
}
export const checkForPermission = (member : Member, permission : string) => {
  member;
  permission;
  return false;
}